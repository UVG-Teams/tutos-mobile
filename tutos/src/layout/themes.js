import { StyleSheet } from 'react-native'

export const theme = StyleSheet.create({
    background: {
        flex: 1,
        width: '100%',
        resizeMode: "cover",
        backgroundColor: '#edf6ff',
    },
    content: {
        padding: 15,
    },
    errorText: {
        color: 'red',
    }
})
